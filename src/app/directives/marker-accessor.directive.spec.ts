import { MarkerAccessorDirective } from './marker-accessor.directive';

describe('MarkerAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkerAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
